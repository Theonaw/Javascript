// formulaire = document.getElementById("formulaire");
// saisie=document.getElementById("saisie");
// message=document.getElementById("message");

// formulaire.addEventListener("submit", afficherMessage);
// function afficherMessage(e){// (e) = evenement
//     e.preventDefault();// annule le comportement par défauly de l'évenement
//     prenom=saisie.value;
//     message.textContent=`Bienvenue ${prenom} sur notre site`;// textContent = ajout de text
// }